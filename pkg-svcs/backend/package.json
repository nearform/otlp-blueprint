{
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "start:container": "npm install && npm run create:env && npm run start",
    "create:env": "rm -rf .env && node -r fs -e \"fs.copyFileSync('.env.sample', '.env', fs.constants.COPYFILE_EXCL)\"",
    "lint": "eslint .",
    "start": "npm run create:env && nodemon --config .nodemonrc .",
    "test": "jest",
    "test:watch": "jest --watch --verbose",
    "test:related": "jest --onlyChanged"
  },
  "dependencies": {
    "@fastify/autoload": "4.0.0",
    "@fastify/cors": "^7.0.0",
    "@fastify/postgres": "^4.0.0",
    "close-with-grace": "^1.1.0",
    "dotenv": "^16.0.0",
    "env-schema": "^4.0.0",
    "fastify": "^3.27.2",
    "fastify-plugin": "^3.0.1",
    "fastify-secrets-aws": "^1.0.0",
    "fastify-secrets-azure": "^1.0.0",
    "fastify-secrets-env": "^1.0.1",
    "fastify-secrets-gcp": "^1.0.1",
    "fluent-json-schema": "^3.1.0",
    "nodemon": "^2.0.16",
    "pg": "^8.7.3",
    "pg-range": "^1.1.1"
  },
  "devDependencies": {
    "faker": "^6.6.6",
    "jest": "^28.1.0",
    "pino-pretty": "^7.5.3",
    "tap": "^16.0.0"
  }
}
